# Test + lint toolchain. The scheduled pipeline workflows install requirements.txt only.
#
# CI installs exactly this file, so lint locally with THIS toolchain rather than a
# globally installed ruff — a version skew between the two is invisible until CI runs.
-r requirements.txt
pytest==9.0.3
pytest-subtests==0.13.1
ruff==0.15.8
