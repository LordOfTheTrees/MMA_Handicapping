# Archived one-off utilities

Scripts kept for reproducibility when the **specific** inputs still exist.

| Script | Purpose |
|--------|---------|
| `merge_k_sensitivity_top15.py` | Merges two Kalman `process_noise` “top 15” text dumps ([`docs/k sensitivity comparison top 15.txt`](../docs/k sensitivity comparison top 15.txt)); format-specific. |

If that doc workflow is obsolete, delete this folder after confirming nothing external references it.
